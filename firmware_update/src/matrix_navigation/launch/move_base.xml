<launch>
    <!--node ns="local_costmap" name="voxel_grid_throttle" pkg="topic_tools" type="throttle" args="messages voxel_grid 3.0 voxel_grid_throttled" /-->
    <arg name="local_planner" default="dwa" doc="Local planner can be either dwa, base, teb or pose"/>
    <arg name="with_virtual_walls" default="true" doc="Enables usage of virtual walls when set. Set to false when running SLAM." />
    <arg name="prefix" default="" doc="Prefix used for robot tf frames" /> <!-- used in the config files -->

    

    <arg name="hw_ver" value="$(env hw_ver)"/>
    <arg name="serial_no" value="$(env serial_no)"/>



        <group if="$(eval hw_ver == 'smr100A')">
            <include file="$(find matrix_navigation)/launch/include/move_base/move_base_2planner_ROHM.xml">
            </include>
        </group>

        <group unless="$(eval hw_ver == 'smr100A')">
            <group if="$(eval serial_no == 'SDR010020230001BAR053')">
                <include file="$(find matrix_navigation)/launch/include/move_base/move_base_TEB_SDR010020230001BAR053.xml">
                </include>
            </group>
            <group unless="$(eval serial_no == 'SDR010020230001BAR053')">
                <include file="$(find matrix_navigation)/launch/include/move_base/move_base_TEB.xml">
                </include>
            </group>
        </group>
    



    

    
    
</launch>
